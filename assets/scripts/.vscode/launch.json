{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Build Honey ScriptCore (Mono)",
      "type": "cppdbg",
      "request": "launch",
      "preLaunchTask": "Build Honey ScriptCore",
      "program": "${workspaceFolder}/Honey-ScriptCore.dll",
      "cwd": "${workspaceFolder}",
      "stopAtEntry": false,
      "console": "integratedTerminal"
    }
  ]
}